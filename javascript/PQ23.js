function concatenation(str){
    let add = "";
    for(let i = 0; i<arr.length; i++){
        add = add + arr[i]
    }
    return add;
}
let arr = ["ritesh", "don" , "No", "1"];

let concate = concatenation(arr);

console.log(concate);